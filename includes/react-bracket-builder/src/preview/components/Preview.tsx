import React from 'react';

// create function component called Preview that takes in a prop called bracket
const Preview = ({ bracket }) => {
    // return a div with a class of preview
    return <div className="preview">Preview</div>;
    };

// export the Preview component
export default Preview;