name: react-bracket-builder workflow

on:
  push:
    branches: ['main']
  pull_request:
    paths:
      - 'react-bracket-builder/**'

jobs:
  test:
    runs-on: [self-hosted]
    steps:
      - uses: actions/checkout@v3
      - name: Setup node
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - run: npm ci --prefix react-bracket-builder
      - run: npm run test --prefix react-bracket-builder
